# --- Core RAG pipeline dependencies ---

# Semantic embeddings and dense cosine retrieval.
sentence-transformers==5.7.0
transformers==5.17.0

# LangChain integrations and LangGraph workflow orchestration
langchain-core>=1.0,<2.0
langchain-community>=0.4,<1.0
langchain-text-splitters>=1.0,<2.0
langchain-huggingface>=1.0,<2.0
langgraph>=1.0,<2.0
faiss-cpu>=1.9,<2.0
rank-bm25>=0.2,<1.0

# OpenAI-compatible client, used to call Gemini's compatible endpoint
openai>=1.50.0

# Document parsing
pypdf==5.1.0
python-docx==1.1.2

# Structured output validation (for the {answer, citation, confidence,...} schema)
pydantic==2.10.3

# Env var loading (.env file for config)
python-dotenv==1.0.1

# Nicer CLI output (tables, colors) for our scripts
rich==13.9.4

# HTTP backend and local static frontend serving
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0

# Automated evaluation tests
pytest>=8.0,<9.0
